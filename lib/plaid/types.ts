// Placeholder — re-exports/narrowing of Plaid SDK types as needed
// (AccountBase, Transaction, LinkTokenCreateResponse, etc. all ship with the
// `plaid` package's own TypeScript types — this file is for any
// app-specific shaping on top of them).
//
// Implementation happens in the Claude Code phase.

export {};
