// Placeholder — balance-runway projection math.
//
// Given current balance, recurring income (mode: "internship" | "school"),
// recurring subscriptions, and average per-category spend, project the
// daily balance forward and return the date the balance crosses zero.
//
// Implementation happens in the Claude Code phase.

export type IncomeMode = "internship" | "school";

export {};
